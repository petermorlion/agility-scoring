<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AgilityScoring.Maui.Views.TournamentDetailPage"
             NavigationPage.HasNavigationBar="False">

    <VerticalStackLayout Padding="16" Spacing="16">
        
        <!-- Tournament Name Label -->
        <Label Text="{Binding TournamentName}"
               FontSize="18"
               FontAttributes="Bold"
               TextColor="{StaticResource TextDark}"
               HorizontalOptions="Center" />

        <!-- Contestant Navigation Bar -->
        <Border BackgroundColor="{StaticResource Primary}" 
                StrokeShape="RoundRectangle 12"
                Stroke="Transparent"
                Padding="16">
            <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="16">
                
                <!-- Previous Button -->
                <Button Grid.Column="0"
                        Text="&#x2190;"
                        FontSize="24"
                        BackgroundColor="Transparent"
                        TextColor="White"
                        WidthRequest="50"
                        HeightRequest="50"
                        Command="{Binding PreviousContestantCommand}"
                        IsEnabled="{Binding CanGoPrevious}" />

                <!-- Contestant Name + Edit -->
                <Grid Grid.Column="1" ColumnDefinitions="*,Auto" VerticalOptions="Center">
                    <Label Grid.Column="0"
                           Text="{Binding ContestantName}"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="White"
                           VerticalOptions="Center"
                           HorizontalOptions="Center" />
                    <Button Grid.Column="1"
                            Text="✏"
                            FontSize="16"
                            BackgroundColor="Transparent"
                            TextColor="White"
                            WidthRequest="40"
                            HeightRequest="40"
                            Clicked="OnRenameContestantClicked" />
                </Grid>

                <!-- Next Button -->
                <Button Grid.Column="2"
                        Text="&#x2192;"
                        FontSize="24"
                        BackgroundColor="Transparent"
                        TextColor="White"
                        WidthRequest="50"
                        HeightRequest="50"
                        Command="{Binding NextContestantCommand}" />

            </Grid>
        </Border>

        <!-- Empty space for future content -->
        <Label Text="(Content area for contestant details)"
               FontSize="16"
               TextColor="{StaticResource TextLight}"
               HorizontalOptions="Center"
               VerticalOptions="Center"
               Margin="0,40,0,0" />

    </VerticalStackLayout>

</ContentPage>
